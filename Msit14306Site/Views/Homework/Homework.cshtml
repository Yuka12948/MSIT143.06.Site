
@{
    ViewData["Title"] = "Homework";
}

@section Styles{

    <style>
        .card-date {
            font-size: 10pt;
            color: gainsboro;
        }

        .btn {
            margin-bottom: 5pt;
        }

        .h2tittle {
            margin-bottom: 20pt;
            font-weight: bold;
            text-align: center;
        }

        .card-columns {
            margin: 0 auto 50pt auto;
        }
    </style>
    <link href="~/css/bootstrap.css" rel="stylesheet" />
}

<h2 class="h2tittle">桃園觀光景點</h2>
<div class="card-columns"></div>
<ul id="ul1"></ul>

@section Scripts{
    <script src="~/js/travel.js"></script>
    <script>
        let htmlDatas = DATAS.map(data => {
            let { Name, Toldescribe, Add, Tel, Opentime, Ticketinfo, Parkinginfo, TYWebsite, Website, Changetime } = data
            return (
                `
                             <div class="card" style="width:22rem margin:auto;">
                               <div class="card-body">
                                 <h4 class="card-title">${Name}</h4>
                                 <p class="card-text">${Toldescribe}</p>
                               </div>
                               <ul class="list-group list-group-flush">
                                 <li class="list-group-item">地址:<br/>${Add}</li>
                                 <li class="list-group-item">電話: ${Tel}</li>
                                 <li class="list-group-item">營業時間: ${Opentime}</li>
                                 <li class="list-group-item">票券資訊: ${Ticketinfo}</li>
                                 <li class="list-group-item">停車資訊: ${Parkinginfo}</li>
                               </ul>
                               <div class="card-body">
                                 <a class="btn btn-primary" href="${Website}" class="card-link">Website</a>
                                 <a class="btn btn-secondary" href="${TYWebsite}" class="card-link">TYWebsite</a><br/>
                                 <span class="card-date">最後更新日期: ${Changetime}</span>
                               </div>
                             </div>
                        `
            )
        })
        document.querySelector(".card-columns").innerHTML = (htmlDatas.join(""))

                    ////讀取資料
                    //const theU1 = document.querySelector("#ul1");

                    ////建立空物件
                    //const docfrag = document.createDocumentFragment()

                    //for (let i = 0; i < DATAS.length; i++) {
                    //    //解構賦值 Destructuring Assignement
                    //    const { Name, TYWebsite, Add, Opentime, Parkinginfo, Ticketinfo, Tel, Fax, Changetime } = DATAS[i]

                    //    //顯示資料 DOM
                    //    //const txtLi = document.createTextNode(emp.employees[i].name)  //建立文字節點 Jack
                    //    const txtLi = document.createTextNode(Name + "　[TEL: " + Tel + "　]　地址: " + Add)
                    //    const eleLi = document.createElement("li") //<li></li>
                    //    eleLi.appendChild(txtLi) //<li>Jack</li>

                    //    docfrag.appendChild(eleLi);
                    //}
                    //theU1.appendChild(docfrag);

    </script>
    <script src="~/js/bootstrap.js"></script>

}